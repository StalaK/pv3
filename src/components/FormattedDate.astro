---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toLocaleDateString('en-GB')}>
	{
		date.toLocaleDateString('en-GB', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
